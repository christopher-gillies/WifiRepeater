#ifndef SECRET_H
#define SECRET_H

/**
 * WiFi Credentials Template
 *
 * INSTRUCTIONS:
 * 1. Copy this file: secret.h.template → secret.h
 * 2. Edit secret.h and replace the placeholder values with your actual credentials
 * 3. secret.h is in .gitignore and will never be committed to version control
 * 4. Never share secret.h publicly or commit it to git
 *
 * IMPORTANT:
 * - secret.h is automatically excluded from git commits (see .gitignore)
 * - Never commit this file to a public repository!
 * - Keep your WiFi credentials safe
 */

// Upstream WiFi Network Credentials
// Replace with your home WiFi SSID and password
#define STA_SSID "YOUR_HOME_WIFI_SSID"
#define STA_PASSWORD "YOUR_HOME_WIFI_PASSWORD"

// Repeater WiFi Network Credentials
// These are what your devices will see when connecting to the repeater
#define AP_SSID "ESP32-Repeater"
#define AP_PASSWORD "repeater123"

#endif // SECRET_H
